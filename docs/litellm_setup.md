# LiteLLM Setup

LiteLLM setup is scheduled for Phase 3.

The reserved local endpoint is:

```text
http://localhost:4000/v1
```

The required model alias is:

```text
qwen3-coder-next-abliterated-h200
```
