# Hugging Face Setup

Hugging Face provides access to the configured coding model and tokenizer files.

For later phases, create a Hugging Face token and store it in Modal Secret `huggingface-secret` using key `HF_TOKEN`.
